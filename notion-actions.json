{
  "openapi": "3.0.0",
  "info": {
    "title": "FINOS Notion Actions API",
    "description": "API for accessing FINOS Notion databases through a Vercel backend. Supports querying rows, retrieving a page, and fetching daily reports.",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "https://finos-gpt-schemas.vercel.app"
    }
  ],
  "paths": {
    "/api/queryDatabase": {
      "post": {
        "summary": "Query rows from a Notion database",
        "operationId": "queryDatabase",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "database_id": {
                    "type": "string",
                    "description": "The Notion database ID to query"
                  },
                  "page_size": {
                    "type": "number",
                    "description": "Number of rows to fetch. Defaults to 1."
                  }
                },
                "required": ["database_id"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Parsed and raw results from the database"
          }
        }
      }
    },
    "/api/retrievePage": {
      "post": {
        "summary": "Retrieve all properties from a Notion page",
        "operationId": "retrievePage",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "page_id": {
                    "type": "string",
                    "description": "The Notion page ID to retrieve"
                  }
                },
                "required": ["page_id"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Parsed and raw properties from the page"
          }
        }
      }
    },
    "/api/dailyReport": {
      "get": {
        "summary": "Retrieve daily error and unknown property report",
        "operationId": "dailyReport",
        "responses": {
          "200": {
            "description": "A summary of issues or '✅ No issues today.'"
          }
        }
      }
    }
  }
}
